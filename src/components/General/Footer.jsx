import React from 'react';
import Logo from './Logo';

const Footer = (props) => {
    return (
        <>
            <footer>
                <Logo />
            </footer>
        </>
    );
};

export default Footer;
