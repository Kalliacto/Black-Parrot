import React from 'react';
import Logo from './Logo';
import './style.css';

const Footer = (props) => {
    return (
        <>
            <footer>
                <Logo />
            </footer>
        </>
    );
};

export default Footer;
