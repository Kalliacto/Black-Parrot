import React from 'react';
import Logo from './Logo';
import './style.css';

const Header = (props) => {
    return (
        <>
            <header>
                <Logo />
            </header>
        </>
    );
};

export default Header;
